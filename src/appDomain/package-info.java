/**
 * The {@code appDomain} package contains the main program entry points
 * and high-level application logic.
 * <p>
 * It includes classes such as {@link appDomain.AppDriver} and {@link appDomain.Program},
 * which handle argument parsing, input validation, and execution control.
 */
package appDomain;