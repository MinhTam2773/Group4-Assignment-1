/**
 * The {@code shapes} package defines various geometric shapes
 * such as cylinders, cones, and pyramids.
 * <p>
 * Each shape implements the abstract {@link shapes.Shape} class,
 * which provides methods to calculate height, base area, and volume.
 */
package shapes;